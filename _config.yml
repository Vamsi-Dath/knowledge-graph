title: "Design Study Lite Handbook"
description: "A step-by-step handbook for running design studies"
baseurl: "/knowledge-graph" # the subpath of your site
url: "https://urban-toolkit.github.io" # the base hostname & protocol for your site

theme: minimal-mistakes-jekyll

plugins:
  - jekyll-include-cache
  - jekyll-redirect-from

# Make README files serve as index pages
plugins:
  - jekyll-readme-index

# Navigation
collections:
  stages:
    output: true
    permalink: /:collection/:path/

defaults:
  - scope:
      path: ""
      type: "stages"
    values:
      layout: single
      sidebar:
        nav: "stages"
